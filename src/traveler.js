class Traveler {
  constructor(id, name, travelerType, tripBook) {
    this.id = id;
    this.name = name;
    this.travelerType = travelerType;
    this.tripBook = tripBook || [];
  }

  calculateAnnualTravelSpending() {
    (acc, trip) => {
      
    }
    this.tripBook.reduce(,0)
  }
}
