class TripBook {
  constructor(tripData, userID) {
    this.userID = userID;
    this.trips = tripData.reduce()
  }
}
(acc, trip) => {
  if(trip.userID === this.userID){
    new Trip(destinationID, departureDate, duration, numberOfTravelers)
  }
  return acc;
}
